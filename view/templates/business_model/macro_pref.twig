{% macro sel_cap(list) %}
<div class="row mt-3 mb-5 text-center">
    <div class="col">
        <select name="invest_cap" class="selectpicker " data-style="btn-dark" dropupAuto="false" title="Select an item" required>
        {% for id,item in list %}
            <option value={{id}}>£ {{item['name']}}</option>
        {% endfor %}
        </select>
    </div>
</div>
{% endmacro %}

{% macro sel_const(list) %}
<div class="row mt-3 mb-5 text-center">
    <div class="col">
        <select name="payback_const" class="selectpicker " data-style="btn-dark" dropupAuto="false" title="Select an item" required>
        {% for id,item in list %}
            <option value={{id}}>{{item['name']}}</option>
        {% endfor %}
        </select>
    </div>
</div>
{% endmacro %}

{% macro sel_bm_pref(list) %}
<div class="row mt-3 mb-5 text-center">
    <div class="col">
        <select name="bm_pref" class="selectpicker " data-style="btn-dark" dropupAuto="false" title="Select an item" required>
        {% for id,item in list %}
            <option value={{id}}>{{item['name']}}</option>
        {% endfor %}
        </select>
    </div>
</div>
{% endmacro %}